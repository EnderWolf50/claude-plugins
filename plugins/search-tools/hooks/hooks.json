{
  "description": "tgrep serve lifecycle (SessionStart start / SessionEnd stop) and a Bash guard that refuses tree searches via grep (grep -r, xargs grep) in favour of rg",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/tgrep-serve.sh\" start",
            "timeout": 20,
            "statusMessage": "tgrep serve check"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/tgrep-serve.sh\" stop",
            "timeout": 20
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/grep-guard.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
